{
  "name": "bartizan-vulnerable-fixture",
  "version": "1.0.0",
  "private": true,
  "description": "Deliberately vulnerable dependencies. Used by CI to verify that every package manager path detects the same advisories.",
  "dependencies": {
    "lodash": "4.17.15",
    "minimist": "1.2.0",
    "axios": "0.21.0"
  }
}
