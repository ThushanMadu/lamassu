{
  "name": "lamassu-clean-fixture",
  "version": "1.0.0",
  "private": true,
  "description": "A project with one tiny, stable, advisory-free dependency. Used by CI to prove lamassu exits 0 on a clean audit - the common case that only vulnerable fixtures never exercise.",
  "dependencies": {
    "ms": "2.1.3"
  }
}
